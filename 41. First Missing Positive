class Solution {
    public int firstMissingPositive(int[] nums) {
      Arrays.sort(nums);
int miss=1;
for (int i = 0; i < nums.length; i++) {
  if (miss==nums[i]) {
    miss+=1;
    
  }
  
}
return miss;
    }
}
